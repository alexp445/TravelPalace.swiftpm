import SwiftUI
struct MiamiFlights: View {
    var body: some View {
        VStack{
            Text("Flights to Miami:")
                .font(.custom("HelveticaNeue-Bold", size: 41))
                .frame(alignment: .top)
                .padding(10)
        }
    }
}
